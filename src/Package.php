<?php

declare(strict_types=1);

namespace FmsOData;

/**
 * Package metadata for fms-odata-php.
 *
 * Composer versions come from VCS tags, so the version is not declared in
 * composer.json. This constant exposes the current client library version
 * for runtime inspection.
 */
final class Package
{
    /** The client library version. */
    public const VERSION = '1.0.0';
}
